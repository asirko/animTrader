language: node_js
node_js:
 - "10"
branches:
  only:
    - master
install:
 - npm install -g firebase-tools
 - npm i
script:
 - firebase use anim-trader
 - npm run build
deploy:
  provider: firebase
  token: $FIREBASE_TOKEN
  project: "anim-trader"
